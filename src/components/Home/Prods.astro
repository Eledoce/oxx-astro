---
import Card from './Card.astro'
---

<section>
  {
    Array(9)
      .fill(5)
      .map((_, i) => <Card key={i} />)
  }
</section>

<style>
  section {
    @apply grid grid-cols-3 gap-3 p-3 w-3/4 mx-auto;
  }
</style>
